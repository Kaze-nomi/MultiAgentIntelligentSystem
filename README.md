# Watermelon Ripeness Evaluator / Система оценки спелости арбузов

![Build Status](https://img.shields.io/badge/build-passing-brightgreen)
![Version](https://img.shields.io/badge/version-1.0.0-blue)
![License](https://img.shields.io/badge/license-MIT-green)

---

## 📖 Описание

Этот проект представляет собой систему для моделирования арбузов и оценки их спелости. Он построен на основе чистой архитектуры с четким разделением на доменную логику и сервисный слой.

Основная цель — предоставить надежный и расширяемый инструмент для определения, готов ли арбуз к употреблению, на основе его физических характеристик.

### 🏗️ Архитектура

Проект использует следующие ключевые компоненты и паттерны проектирования:

-   **Компоненты:**
    -   `Watermelon`: Модель домена, представляющая арбуз с его физическими свойствами.
    -   `RipenessEvaluator`: Интерфейс (протокол) для сервисов оценки спелости.
    -   `RipenessStatus`: Перечисление, описывающее возможные состояния спелости.
-   **Паттерны:**
    -   **Strategy**: Позволяет подменять алгоритмы оценки спелости.
    -   **Data Transfer Object (DTO) / Value Object**: `Watermelon` используется как объект для передачи данных.
    -   **Builder**: Создание объектов `Watermelon` через Pydantic следует принципам строителя.

---

## 📋 Содержание

-   [✨ Возможности](#-возможности)
-   [🚀 Установка](#-установка)
-   [⚙️ Конфигурация](#️-конфигурация)
-   [📚 Использование](#-использование)
-   [🔌 API Reference](#-api-reference)
-   [🤝 Внесение вклада](#-внесение-вклада)
-   [📄 Лицензия](#-лицензия)

---

## ✨ Возможности

-   **Надежное моделирование данных**: Использование Pydantic для валидации и сериализации данных об арбузе.
-   **Расширяемая логика оценки**: Паттерн `Strategy` позволяет легко добавлять новые алгоритмы определения спелости.
-   **Чистая архитектура**: Четкое разделение ответственности между доменом (`domain`) и сервисами (`service`).
-   **Полная типизация**: Код полностью покрыт type hints для улучшения читаемости и поддержки.
-   **Высокое тестовое покрытие**: Включены комплексные тесты для доменных моделей и сервисов.

---

## 🚀 Установка

### Требования

-   Python 3.8+

### Установка из исходного кода

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/your-repo/watermelon-evaluator.git
    cd watermelon-evaluator
    ```

2.  Создайте и активируйте виртуальное окружение:
    ```bash
    python -m venv venv
    source venv/bin/activate  # для Windows: venv\Scripts\activate
    ```

3.  Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```

4.  Установите проект в режиме разработки:
    ```bash
    pip install -e .
    ```

### Установка с помощью Docker

1.  Соберите Docker-образ:
    ```bash
    docker build -t watermelon-evaluator .
    ```

2.  Запустите контейнер:
    ```bash
    docker run -it --rm watermelon-evaluator
    ```

---

## ⚙️ Конфигурация

Проект может быть сконфигурирован через переменные окружения. В настоящее время доступна следующая опция:

-   `WATERMELON_EVALUATOR_STRATEGY`: Указывает, какую стратегию оценки использовать по умолчанию (например, `heuristic`).

---

## 📚 Использование

### Создание модели арбуза

Для начала создайте экземпляр класса `Watermelon`, указав его характеристики.

```python
from src.my_project.domain import Watermelon, RipenessStatus

# Создаем арбуз с весом 7.5 кг и размером 25 см
my_watermelon = Watermelon(weight_kg=7.5, diameter_cm=25, tail_color='dry')

print(f"Создан арбуз: {my_watermelon}")
# > Создан арбуз: weight_kg=7.5 diameter_cm=25 tail_color='dry'
```

### Оценка спелости

Используйте сервис оценки для определения состояния арбуза.

```python
from src.my_project.service import RipenessEvaluator, get_default_evaluator

# Получаем экземпляр оценщика по умолчанию
evaluator: RipenessEvaluator = get_default_evaluator()

# Оцениваем спелость созданного арбуза
status = evaluator.evaluate(my_watermelon)

print(f"Статус спелости: {status.name}")
# > Статус спелости: RIPE

# Проверяем статус
if status == RipenessStatus.RIPE:
    print("Арбуз готов к употреблению!")
elif status == RipenessStatus.UNRIPE:
    print("Арбуз еще не дозрел.")
else:
    print("Арбуз перезрел.")
```

---

## 🔌 API Reference

### `Watermelon`

Основная модель домена, представляющая арбуз.

**Атрибуты:**
-   `weight_kg` (`float`): Вес арбуза в килограммах.
-   `diameter_cm` (`float`): Диаметр арбуза в сантиметрах.
-   `tail_color` (`str`): Цвет хвостика арбуза (например, 'green', 'dry', 'brown').

### `RipenessStatus`

`Enum` для представления статуса спелости.

**Значения:**
-   `UNRIPE`: Недозрелый.
-   `RIPE`: Спелый.
-   `OVERRIPE`: Перезрелый.

### `RipenessEvaluator`

Протокол (интерфейс) для всех сервисов оценки спелости.

**Методы:**
-   `evaluate(watermelon: Watermelon) -> RipenessStatus`:
    Принимает объект `Watermelon` и возвращает его `RipenessStatus`.

---

## 🤝 Внесение вклада

Мы приветствуем вклад в развитие проекта! Пожалуйста, следуйте следующим шагам:

1.  Сделайте `fork` репозитория.
2.  Создайте новую ветку для вашей функции (`git checkout -b feature/amazing-feature`).
3.  Внесите изменения и добавьте тесты для них.
4.  Убедитесь, что все тесты проходят (`pytest`).
5.  Сделайте коммит ваших изменений (`git commit -m 'Add some amazing feature'`).
6.  Отправьте изменения в ваш `fork` (`git push origin feature/amazing-feature`).
7.  Откройте `Pull Request`.

---

## 📄 Лицензия

Этот проект лицензирован под лицензией MIT. Подробности см. в файле [LICENSE](LICENSE).