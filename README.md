# КОМПИЛЯТОРЫ

Завтра

[![Build Status](https://img.shields.io/badge/build-passing-brightgreen)](https://github.com/your-repo/actions)
[![Version](https://img.shields.io/badge/version-1.0.0-blue)](https://github.com/your-repo/releases)
[![License](https://img.shields.io/badge/license-MIT-green)](LICENSE)

## 📋 Table of Contents
- [Описание](#описание)
- [Функциональность](#функциональность)
- [Установка](#установка)
- [Использование](#использование)
- [Конфигурация](#конфигурация)
- [API](#api)
- [Вклад](#вклад)
- [Лицензия](#лицензия)

## 📦 Описание
Проект "КОМПИЛЯТОРЫ" представляет собой набор инструментов для компиляции и обработки задач. В рамках проекта добавлен **Black Hole Agent** — симулятор агента, который принимает задачи и имитирует их "поглощение" (абсорбцию). Это экспериментальный компонент, реализованный на Python, для демонстрации обработки асинхронных задач.

## 🚀 Функциональность
- **Компиляция задач**: Основная функция проекта — обработка и компиляция различных задач.
- **Black Hole Agent**: Новый агент, который:
  - Принимает задачи через сервер.
  - Симулирует поглощение задач (абсорбцию) для демонстрационных целей.
  - Реализован в файле `agents/black_hole_agent/server.py` на Python.
- **Интеграция с Docker**: Поддержка контейнеризации для лёгкого развёртывания.

## ⚙️ Установка
1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/your-repo/compilers.git
   cd compilers
   ```

2. Установите зависимости (если применимо):
   ```bash
   pip install -r requirements.txt
   ```

3. Для Black Hole Agent используйте Docker:
   ```bash
   docker build -t black-hole-agent .
   docker run -p 8080:8080 black-hole-agent
   ```

## 📖 Использование
### Основное использование
Запустите основной компилятор:
```bash
python main.py
```

### Black Hole Agent
Запустите сервер агента:
```python
from agents.black_hole_agent.server import BlackHoleServer

server = BlackHoleServer()
server.start()
```

Пример отправки задачи на сервер (используя curl):
```bash
curl -X POST http://localhost:8080/absorb -d '{"task": "example task"}'
```

Сервер симулирует поглощение и возвращает подтверждение.

## 🔧 Конфигурация
Конфигурация осуществляется через переменные окружения или файл `config.yaml`. Для Black Hole Agent:
- `PORT`: Порт сервера (по умолчанию 8080).
- `ABSORPTION_DELAY`: Задержка симуляции поглощения в секундах (по умолчанию 1).

Пример `config.yaml`:
```yaml
black_hole_agent:
  port: 8080
  absorption_delay: 1
```

## 📚 API
### Black Hole Agent
- **POST /absorb**: Принимает задачу и симулирует поглощение.
  - Вход: JSON с полем `task` (строка).
  - Выход: JSON с подтверждением `{"status": "absorbed", "task": "example task"}`.

Пример запроса:
```python
import requests

response = requests.post('http://localhost:8080/absorb', json={'task': 'my task'})
print(response.json())
```

## 🤝 Вклад
Мы приветствуем вклад! Пожалуйста, следуйте этим шагам:
1. Форкните репозиторий.
2. Создайте ветку для вашей фичи: `git checkout -b feature/new-feature`.
3. Зафиксируйте изменения: `git commit -m 'Add new feature'`.
4. Отправьте pull request.

## 📄 Лицензия
Этот проект лицензирован под MIT License. См. файл [LICENSE](LICENSE) для деталей.