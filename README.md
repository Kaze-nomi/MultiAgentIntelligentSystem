# 🥯 My Project

[![Build Status](https://img.shields.io/badge/build-passing-brightgreen.svg)](https://github.com/your-repo/my-project)
[![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)](https://github.com/your-repo/my-project/releases)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

## 📑 Содержание

- [📦 Описание](#-описание)
- [✨ Возможности](#-возможности)
- [🚀 Установка](#-установка)
- [💡 Использование](#-использование)
- [⚙️ Конфигурация](#️-конфигурация)
- [📚 API Reference](#-api-reference)
- [🤝 Внесение вклада](#-внесение-вклада)
- [📄 Лицензия](#-лицензия)

## 📦 Описание

My Project - это современное приложение для работы с бубликами. Проект использует Python и Pydantic для создания надёжных и валидированных моделей данных.

## ✨ Возможности

- 🥯 Создание и управление бубликами
- ✅ Валидация данных с помощью Pydantic
- 🧪 Полное тестовое покрытие
- 🐳 Поддержка Docker и Docker Compose
- 🔗 Интеграция с OpenRouter API

## 🚀 Установка

### Требования

- Python 3.8+
- Docker (опционально)

### Установка через pip

```bash
pip install my-project
```

### Установка из исходников

```bash
git clone https://github.com/your-repo/my-project.git
cd my-project
pip install -e .
```

### С помощью Docker

```bash
docker-compose up -d
```

## 💡 Использование

### Создание бублика

```python
from my_project.domain import Bublik

# Создание нового бублика
bublik = Bublik(
    name="Сладкий бублик",
    flavor="ванильный",
    is_tasty=True,
    price=99.99
)

print(bublik)
# > name='Сладкий бублик' flavor='ванильный' is_tasty=True price=99.99
```

### Валидация данных

```python
from pydantic import ValidationError

try:
    invalid_bublik = Bublik(
        name="",  # Пустое имя вызовет ошибку
        flavor="шоколадный",
        is_tasty=True,
        price=-10  # Отрицательная цена вызовет ошибку
    )
except ValidationError as e:
    print(f"Ошибка валидации: {e}")
```

### Сериализация в JSON

```python
bublik_json = bublik.model_dump_json()
print(bublik_json)
# > {"name":"Сладкий бублик","flavor":"ванильный","is_tasty":true,"price":99.99}
```

## ⚙️ Конфигурация

Проект поддерживает конфигурацию через переменные окружения:

```bash
export MY_PROJECT_DEBUG=true
export MY_PROJECT_API_KEY=your-api-key
export OPENROUTER_API_KEY=your-openrouter-key
```

## 📚 API Reference

### Bublik

Основная модель для представления бубликов.

#### Параметры

- `name` (str): Название бублика (обязательно)
- `flavor` (str): Вкус бублика (опционально)
- `is_tasty` (bool): Флаг indicating вкусность (по умолчанию: True)
- `price` (float): Цена бублика (должна быть положительной)

#### Методы

- `model_dump()`: Возвращает словарь с данными модели
- `model_dump_json()`: Возвращает JSON-строку с данными модели
- `model_validate()`: Валидирует данные и создаёт экземпляр модели

## 🤝 Внесение вклада

Мы приветствуем вклад в развитие проекта! Пожалуйста, следуйте этим шагам:

1. Форкните репозиторий
2. Создайте ветку для вашей фичи (`git checkout -b feature/amazing-feature`)
3. Закоммитьте ваши изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Тестирование

```bash
# Запуск всех тестов
pytest

# Запуск тестов с покрытием
pytest --cov=src/my_project

# Запуск конкретного тестового файла
pytest tests/test_domain/test_bublik.py
```

## 📄 Лицензия

Этот проект лицензирован под MIT License. Подробности см. в файле [LICENSE](LICENSE).

---

**Завтра** - компиляторы будут ещё лучше! 🚀